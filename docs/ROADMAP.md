# 実装ロードマップ

設計 ([DESIGN.md](./DESIGN.md)) を段階的に実装するための計画。
各マイルストーンは**それ単体で動作を確認できる**単位に切ってある。

参照作品 [Little LUMI Model](https://store.steampowered.com/app/5075020/Little_LUMI_Model/) を踏まえ、
**「デスクトップを地形として扱う」を早い段階（M3）に置いている。** ここが体験の芯であり、
後回しにすると物理・状態機械を作り直すことになるため。

---

## M0 — 土台

**目標**: 空のプロジェクトが `npm run dev` で起動し、CI が通る。

- [ ] electron-vite + TypeScript(strict) + ESLint/Prettier の雛形
- [ ] `src/shared/types` と `src/shared/ipc/channels.ts` の骨組み
- [ ] `Clock` / `Rng` インターフェース（DESIGN.md §20 のテスト決定論化の前提）
- [ ] Vitest 設定、GitHub Actions（lint + typecheck + unit）
- [ ] electron-builder 設定（NSIS + portable、未署名）
- [x] ドキュメント／パック整合性チェック（`scripts/check-docs.py`）— 内部リンクと検証 V3–V14 のうち静的に効くものを検査済み。`npm run check:docs` として CI に載せる

**完了条件**: 何も表示されないが常駐し、トレイから終了できる。

---

## M1 — 見えるようにする

**目標**: 透過オーバーレイにキャラが立ち、クリックが背後に抜ける。

- [ ] `OverlayWindowManager`: ディスプレイ毎の透過・クリックスルー窓（DESIGN.md §5.1）
- [ ] PixiJS 初期化、`AnimationPlayer`
- [ ] `PackManager` + zod スキーマ + `AlphaMaskBuilder`（読込時のマスク事前展開）
- [ ] `HitTester`（per-pixel + ヒステリシス、DESIGN.md §5.3）
- [ ] **作画 段階1**: 待機・歩行・つかむ・落ちる（`packs/luna/sprites/README.md` の「描く順番」参照）
- [ ] 適応フレームレート（DESIGN.md §6.4）

**完了条件**: ルナが `idle` で待機し、体の上でだけカーソルが効く。透明部分のクリックは背後のアプリに通る。

**ここが本プロジェクト最大の技術リスク。** 動かないと分かった場合、この時点なら方式変更（小窓移動方式への退避）の傷が浅い。
GPU ちらつきの検証を複数マシンで行うこと。

---

## M2 — 動く・掴める・なでられる

**目標**: 床の上で生き物らしく振る舞い、触って気持ちいい。

- [ ] `BehaviorFSM`（重み付き遷移、優先度調停、DESIGN.md §9.1–9.2）
- [ ] `Physics`（固定タイムステップ、重力・反発・摩擦、DESIGN.md §8）
- [ ] `TerrainMap` の骨格（**この段階ではディスプレイ由来の床のみ**）
- [ ] `DragController`（投げ速度のサンプリング、ポインタキャプチャ）
- [ ] **`GestureRecognizer`（なで・ふる、DESIGN.md §10.1–10.2）**
- [ ] `TrayController` と最小限のメニュー（呼ぶ / 隠す / 終了）
- [ ] マルチディスプレイの Actor Handoff（DESIGN.md §4.1）
- [ ] **作画 段階3**: なでなで3段階・ふる

**完了条件**: 歩き、座り、寝る。掴んで投げるとタスクバーの上に着地する。頭を撫でると3段階で反応が変わる。2 画面を跨いで歩ける。

**なでの認識率をここで詰める。** ヒットテストのヒステリシス幅（既定 3px）と
ストローク閾値（6dip / 0.6秒 / 2ストローク）は実測で調整する。軌跡データを回帰テスト資産として保存しておく。

---

## M3 — デスクトップを地形にする

**目標**: 参照作品の芯。ウィンドウを登り、ぶら下がり、意味もなく飛び降りる。

- [ ] `Win32Bridge`（koffi: `EnumWindows` / `GetWindowRect` / `GetForegroundWindow` / `GetWindowLong` / `DwmGetWindowAttribute`）
- [ ] `TerrainService`: ウィンドウ矩形 → 4 種の面（DESIGN.md §7.1–7.2）
- [ ] **除外フィルタ**（DWM クローク・ツールウィンドウ・最小化・自窓）— これを怠ると地形がゴミだらけになる
- [ ] 遮蔽フィルタ、差分送信（`terrain:patch`）
- [ ] `Locomotion`: 接着（stand / cling / hang）と面に沿った移動（DESIGN.md §7.4）
- [ ] **コーナー遷移**（乗り換え / 角を曲がる / dive / 引き返す、DESIGN.md §7.5）
- [ ] 面の移動追従と消滅時の落下（DESIGN.md §7.6）
- [ ] `ForegroundWatcher` + 全画面検出と自動退避（DESIGN.md §9.6）
- [ ] **作画 段階2**: `climb` / `hang` / `hangMove` / `cornerOut` / `cornerIn` / `turn` / `dive`

**完了条件**: エディタのタイトルバーの上を歩き、側面を登り、下端にぶら下がる。
たまに意味もなく飛び降りる。乗っている窓をドラッグすると付いてきて、速く振ると落ちる。窓を閉じると落下する。
ゲームを全画面で起動すると消え、終了で戻る。

**リスク**: koffi の呼出でアンチウイルス誤検知の可能性。
この機能群は設定「ウィンドウに乗る」で丸ごと OFF にでき、**OFF でも M0–M2 の全機能が床の上で動く**よう分離しておく。

**プライバシー**: ウィンドウ列挙は「今何をしているか」を映す。**矩形と hwnd 以外を保持しない**実装規約を
この段階で確立し、以降のレビュー観点に固定する（DESIGN.md §16）。

---

## M4 — 状況に反応する

**目標**: 参照作品には無い独自要素を入れる。

- [ ] `SensorHub` + 各ソース（CPU / メモリ / バッテリ / 時刻 / 無操作 / セッション / ネットワーク / 前景アプリ）
- [ ] `Normalizer`（EMA・シュミットトリガ・エッジ検出、DESIGN.md §13.2）
- [ ] `ReactionEngine` + 条件 DSL 評価器
- [ ] `Governor`（クールダウン・発話上限・静音モード、DESIGN.md §13.3）
- [ ] `SpeechBubble`（DOM 層、文字送り、画面端反転、**壁・天井での向き調整**）
- [ ] `dialogue` のプレースホルダ展開
- [ ] アプリカテゴリ分類表
- [ ] **作画 段階4**: 感情・リアクション

**完了条件**: CPU を張り付かせると汗をかいて喋る。放置して戻ると「おかえり」と言う。1 時間の発話が 6 回を超えない。

**この段階で必ず「1 日使ってみる」こと。** うるささの調整は実測でしかできない。
ここで妥協すると、機能を足すほど嫌われるアプリになる。

---

## M5 — 育てる・時間を測る

**目標**: 長く一緒にいる理由を作る。

- [ ] `AffinityService`（6 段階、加点上限、日付跨ぎ、DESIGN.md §11）
- [ ] `AffinityStore`（`affinity.json`、設定とは別ファイル）
- [ ] セリフの `minStage` 解禁、状態の `minStage`
- [ ] `TimerService`: ポモドーロ（自律的な作業/休憩の切替、DESIGN.md §12.1）
- [ ] アラーム（3 段階の主張度、サイレン + 走り回り、スヌーズ）
- [ ] Governor の Pri 3 バイパス、静音モードの尊重
- [ ] 進捗バー表示
- [ ] **作画 段階5**: 休息・だいすき度解禁モーション

**完了条件**: 日付を跨いで起動すると連続日数が増え、なでるとスコアが上がる（上限つき）。
ポモドーロ作業中は静かになり、休憩開始で確実に割り込む。アラーム「おおごと」でサイレンと走り回りが発火する。

---

## M6 — 使えるようにする

**目標**: 他人に配れる状態。

- [ ] 設定ウィンドウ（React、全タブ、DESIGN.md §14.3）
- [ ] 設定の永続化とマイグレーション
- [ ] 自動起動、体数変更、キャラ切替
- [ ] パックのインストール UI（zip / `.lunapack` の D&D、フォルダを開く、再読込、検証エラー一覧）
- [ ] キャラ右クリックメニュー
- [ ] 性能予算の実測と是正（DESIGN.md §15）
- [ ] リリース前チェックリストの消化（DESIGN.md §21）
- [ ] インストーラの署名、README と配布ページ

**完了条件**: インストーラをダウンロードした人が、設定を一度も開かずに満足できる。

---

## M7 以降 — 任意

優先度順に並べるが、いずれも M6 の品質を落としてまで入れない。

| 案 | 概要 | 備考 |
|----|------|------|
| 追加パック | 2 体目以降のキャラ | 仕様の実証にもなる |
| パック作成ツール | アトラス生成 + `mascot.json` の GUI 編集 + プレビュー | 拡張性(P3)を本当に活かすなら必要 |
| キャラ同士の相互作用 | 複数体が近づくと反応する | 状態機械の拡張が要る |
| 英語ロケール | UI と `dialogue/en.json` | |
| AI 会話 | 吹き出しから対話 | **要検討**: 通信が発生するため DESIGN.md §16 のプライバシー方針を再設計する必要がある。既定 OFF・明示同意は必須 |
| 自動更新 | electron-updater | 署名が前提 |

---

## 進め方の原則

1. **M1 のクリックスルー**と **M3 のコーナー遷移**が二大リスク。どちらも早めに実機で潰す。
2. 各マイルストーンの完了条件は**実機で目視確認**する。ユニットテストは通っても体験は分からない。
3. **作画をエンジンと並行して進める。** 参照作品が 122 フレームを手描きしているのは、
   枚数こそが体感品質だからで、エンジンの機能を増やしても絵が足りなければ安っぽいままになる。
4. M4 完了後に一度立ち止まり、自分で常用して「うるさくないか」を確認する。
